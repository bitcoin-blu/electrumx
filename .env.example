# ElectrumX Server Configuration
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed

# REQUIRED: Coin name (e.g., Bitcoin, BitcoinTestnet, Litecoin)
COIN=BitcoinBlu

# REQUIRED: Network (mainnet, testnet, regtest, etc.)
NET=main-blu

# REQUIRED: Database directory path (inside container)
DB_DIRECTORY=/var/lib/electrumx

# REQUIRED: Bitcoin daemon RPC URL
# Format: http://username:password@hostname:port/
# For macOS Docker: use host.docker.internal instead of localhost
# For Linux Docker: use host IP or 172.17.0.1
DAEMON_URL=http://rpcuser:rpcpassword@host.docker.internal:8342/

# Services to offer (comma-separated)
# tcp://:50001 - Plain TCP on port 50001
# ssl://:50002 - SSL on port 50002 (requires SSL_CERTFILE and SSL_KEYFILE)
# rpc:// - RPC on localhost:8000
SERVICES=tcp://:50001,rpc://

# Services to advertise (for peer discovery)
# Set to your public hostname/IP if serving publicly
# REPORT_SERVICES=tcp://yourdomain.com:50001,ssl://yourdomain.com:50002

# SSL Configuration (required if using ssl:// or wss:// in SERVICES)
# SSL_CERTFILE=/path/to/certificate.crt
# SSL_KEYFILE=/path/to/private.key

# Database engine (leveldb or rocksdb)
DB_ENGINE=leveldb

# Cache size in MB (default: 1200, max recommended: 2000)
CACHE_MB=1200

# Allow running as root (needed for Docker)
ALLOW_ROOT=true

# Optional: Donation address
# DONATION_ADDRESS=your-bitcoin-address

# Optional: Banner file path
# BANNER_FILE=/path/to/banner.txt

# Optional: Log level (debug, info, warning, error)
LOG_LEVEL=info

# Optional: Maximum sessions (default: 1000)
# MAX_SESSIONS=1000

# Optional: Maximum send size in bytes (default: 8100000)
# MAX_SEND=8100000
